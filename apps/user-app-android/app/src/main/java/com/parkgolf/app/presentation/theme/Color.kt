package com.parkgolf.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Primary Colors
val ParkPrimary = Color(0xFF10B981)          // Emerald
val ParkSecondary = Color(0xFF059669)         // Dark Emerald
val ParkAccent = Color(0xFFF59E0B)            // Amber (Premium)

// Semantic Colors
val ParkSuccess = Color(0xFF22C55E)
val ParkWarning = Color(0xFFEAB308)
val ParkError = Color(0xFFEF4444)
val ParkInfo = Color(0xFF3B82F6)

// Status Colors
val StatusConfirmed = Color(0xFF22C55E)
val StatusPending = Color(0xFFF59E0B)
val StatusCancelled = Color(0xFFEF4444)
val StatusCompleted = Color(0xFF6B7280)

// Glass Effect Colors
val GlassBackground = Color.White.copy(alpha = 0.1f)
val GlassBorder = Color.White.copy(alpha = 0.2f)
val GlassCard = Color.White.copy(alpha = 0.15f)
val GlassCardDark = Color.White.copy(alpha = 0.08f)

// Background Gradient Colors
val GradientStart = Color(0xFF065F46)
val GradientMiddle = Color(0xFF047857)
val GradientEnd = Color(0xFF10B981)

// Text Colors
val TextPrimary = Color.White
val TextSecondary = Color.White.copy(alpha = 0.7f)
val TextTertiary = Color.White.copy(alpha = 0.5f)

// Surface Colors
val SurfaceDark = Color(0xFF1F2937)
val SurfaceLight = Color(0xFFF9FAFB)
