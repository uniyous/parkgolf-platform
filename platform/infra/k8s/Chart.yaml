apiVersion: v2
name: parkgolf-platform
description: 🟨 platform 제품 차트 — 공유 코어(iam·notify·weather·location·job) + 공유 인프라(NATS·PostgreSQL·config·secrets·Ingress)
type: application
version: 0.1.0
dependencies:
  - name: parkgolf-lib
    version: 0.1.0
    repository: "file://../../../shared/charts/parkgolf-lib"
